class CImages {
  CImages({this.image});
  String image;
}

final List<CImages> _cImages = <CImages>[
  CImages(image: 'assert/images/img1'),
  CImages(image: 'assert/images/img2'),
  CImages(image: 'assert/images/img3'),
  CImages(image: 'assert/images/img4'),
];